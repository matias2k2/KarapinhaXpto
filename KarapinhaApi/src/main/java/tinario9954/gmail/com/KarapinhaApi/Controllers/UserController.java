package tinario9954.gmail.com.KarapinhaApi.Controllers;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import tinario9954.gmail.com.KarapinhaApi.Services.UserService;

//@RequestMapping("/users")
//@RestController
public class UserController {
    /*
     * private final UserService userService;
     * 
     * public UserController(UserService userService) {
     * this.userService = userService;
     * }
     */
    /*
     * @GetMapping
     * public ResponseEntity<Page<UsersDTO>> findAll(
     * 
     * @RequestParam(value = "page", defaultValue = "0") Integer page,
     * 
     * @RequestParam(value = "linesPerPage", defaultValue = "12") Integer
     * linesPerPage,
     * 
     * @RequestParam(value = "direction", defaultValue = "ASC") String direction,
     * 
     * @RequestParam(value = "orderBy", defaultValue = "firstName") String orderBy
     * 
     * ) {
     * PageRequest _pageRequest = PageRequest.of(page, linesPerPage,
     * Direction.valueOf(direction), orderBy);
     * Page<UsersDTO> lista = userService.findAllPaged(_pageRequest);
     * return ResponseEntity.ok().body(lista);
     * }
     */

}
